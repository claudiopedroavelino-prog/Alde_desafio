## Passos

1. Instale dependências no terminal:
   ```bash

   pip install flask
   pip install flask_jwt_extended 

2. baixa e instala o node js
   abri a terminal e instala o three js
   --> npm install three

3. Rode o servidor Flask:
   abre o terminal e digite:
   python.exe app.py
   ```

4. Abra no navegador:
   ```
   http://127.0.0.1:5000
   ```

5. roda o teste:
   python.exe test.py

6. OBS:
       para rodar o teste , primeiro tens que fechar o servidor do flask
       depois podes digitar no terminal : python.exe test.py